# 변경 이력 (Changelog)

## v0.9.0 — 2026-09-12

### 🐛 수정
- **녹취록이 없는 세션을 열면 앱에 내장된 예제 대화가 그 세션의 기록인 것처럼 표시되던 문제** — 제목·언어쌍과 전혀 다른 대화가 나왔습니다. 이제 왜 비어 있는지 알려주고, 녹음이 남아 있으면 "이 녹음 다시 전사"로 대본을 만들 수 있다고 안내합니다. 녹취록이 없을 때는 AI 재번역 토글도 함께 숨깁니다(없는 것을 조작하는 컨트롤 제거)

### ✨ 변경
- **첫 실행에 예제 세션 4건을 만들지 않습니다.** 발화 수·길이·미리보기가 전부 지어낸 값이라 실제 기록과 구분할 단서가 없었고, 열면 제목과 다른 대화가 나왔습니다. 홈의 "최근 세션"은 기록 보관함과 같은 톤의 빈 상태 안내를 표시합니다
- **라이선스를 소스 공개형으로 개정했습니다.** 개인적·비상업적 목적의 다운로드·열람·빌드·실행과 그에 필요한 수정을 허용하고, 재배포·공개 배포·판매·상업적 이용과 수정본 배포는 계속 금지합니다. 배포 바이너리가 없어 clone 후 빌드가 유일한 사용 방법인데 구 조항은 복제 자체를 금지하고 있어 서로 어긋났습니다
- 저장소를 공개(public)로 전환했습니다

### 📖 문서
- README에 **화면 미리보기**(홈 · 녹취록 · 회의록)와 **사용법**을 추가했습니다 — 언어 선택부터 대화 방식, 라이브 화면 조작, 세션 상세, 기록 보관함, 설정까지 순서대로
- 버전별 **변경 이력 표**(v0.1.0 ~ v0.9.0)를 README에 넣었습니다
- **빌드 안내 정정** — 프로젝트에 개발자 본인의 서명 인증서 이름이 기본값으로 들어 있어, 다른 기기에서는 `CODE_SIGN_IDENTITY="-"`(또는 `VV_SIGN_IDENTITY="-"`) 없이는 빌드가 실패했습니다
- **표기 정정** — Swift 언어 모드(5.0), 모델을 포함해 빌드했을 때의 설치 용량(약 5 GB), 실사용 검증 범위 고지
- 소스 주석에서 저장소에 없는 내부 문서를 가리키던 참조를 정리했습니다(판단 근거와 실측 수치는 그대로, 코드 변경 없음)

### 🧹 정리
- `.gitignore`에 인증서·프로비저닝 프로파일·`.env` 차단 추가
- 프로젝트 파일에 중복으로 들어 있던 버전 키 정리(`MARKETING_VERSION`·`CURRENT_PROJECT_VERSION` 각 2회 → 1회)

## v0.8.0 — 2026-09-11

### ✨ 새 기능
- **세션 상세에서 화자 바로잡기** — 녹취록의 화자 칩을 클릭해 `나` · `상대 N` · 새 화자로 바꿀 수 있습니다. "같은 화자의 모든 줄에 적용" 토글로 한 번에 정정합니다. 직접 지정한 줄은 이후 자동 재계산이 덮어쓰지 않습니다
- **화자 다시 인식** — 세션 음원(원음)으로 종료 후 화자를 정밀 재계산합니다. 인원 자동 추정 또는 2~5명 지정. 결과(감지 인원·배정 줄 수·보존 줄 수)는 세션 기록 조건에 남습니다
- **이 녹음 다시 전사** — 녹음 전체를 게이트·잠정 없이 통째로 전사한 대본을 "재전사 대본" 탭에 보여줍니다. 라이브 자막이 놓친 말을 대조하는 용도이며 원래 녹취록은 바뀌지 않습니다
- **치환 규칙**(설정) — 인식기가 자주 틀리는 이름·브랜드·전문용어를 등록하면 화면과 내보내기에서 바로잡습니다. 저장된 원문은 그대로 두고, 줄에 마우스를 올리면 원문이 보입니다. 한글은 조사가 붙어도(정크성**은** → 접근성은) 치환되고, 라틴 문자는 단어 경계를 지킵니다(cat이 category를 건드리지 않음)
- **회의록 다시 만들기** — 실패했거나 Apple Intelligence를 나중에 켠 세션에서 다시 시도합니다. 직접 정정한 참여 인원 이름은 유지됩니다. 실패 사유가 "Apple Intelligence 꺼짐 / 모델 준비 중 / 기기 미지원"으로 구분됩니다
- **내보내기 확장** — JSON 형식 추가, "녹취록 포함" 토글(회의록만 한 장으로), "군말 제거"(음·어·저기·um·uh — 파일에서만, 저장·화면 불변)
- **쉼 없이 이어지는 말을 문장 단위로 먼저 확정(실험)** — 두 사람이 쉼 없이 주고받을 때 4문장이 한 줄로 뭉치고 언어가 섞이던 현상을 줄입니다. 설정 › 인식(실험)에서 켜며 다음 세션부터 반영됩니다(기본 꺼짐)
- **언어 선택 기억** — 앱을 다시 열어도 마지막에 고른 언어 1·2가 유지됩니다(이전에는 매번 한국어·한국어로 돌아갔습니다)

### 🐛 수정
- **저장된 세션이 전부 "오늘"로 표시되던 문제** — 세션에 실제 날짜가 없어 생긴 결함입니다. 이제 시작 시각을 저장해 오늘 · 어제 · M. d HH:mm 로 표시하고, 옛 세션은 녹음 파일 생성 시각으로 복구를 시도하며 복구할 수 없으면 "날짜 미상"으로 정직하게 표기합니다
- **말소리를 못 찾은 세션이 아무 안내 없이 홈으로 돌아가던 문제** — 이제 "말소리를 찾지 못했습니다"와 녹음 보존 여부를 홈에 1회 알립니다
- **세션 저장 실패가 조용히 넘어가던 문제** — 디스크 부족 등으로 저장에 실패하면 어느 화면에서든 배너로 알립니다
- **실시간 번역 실패가 보이지 않던 문제** — 연속 3회 이상 실패하면 배너로 알리고(회복되면 사라짐), 밀린 번역은 200줄 상한에서 오래된 것부터 건너뛰며 건너뛴 수를 표시합니다. 실패 줄 수는 세션 기록 조건에 남습니다
- **AI 재번역 일부 실패가 "완료"로만 보이던 문제** — "N줄 중 M줄은 AI 재번역을 만들지 못했습니다"로 표기합니다
- **세션 기록 파일을 읽지 못했을 때 예제 세션이 대신 나타나던 문제** — 이 상태에서 무엇이든 바꾸면 예제가 실제 기록을 덮어쓸 수 있었습니다. 이제 원본을 `sessions.unreadable-<시각>.json`으로 보존하고 빈 목록과 안내 배너로 시작합니다
- **사이드바·홈·기록 보관함의 세션 행이 보조 기술(VoiceOver)로 눌리지 않던 문제** — 진짜 버튼으로 바꿨습니다
- 환각 방어 사전에 한국어 항목 4종 추가("구독과 좋아요 부탁해요", "음악 소리", "웃음 소리", "박수 소리")

### ✨ 변경
- 세션 상세 헤더에 **어느 인식기로 만든 세션인지**(WhisperKit / Apple SpeechTranscriber)를 표시하고, 세션 기록 조건(마이크 실패 · 오디오 끊김 복구 횟수 · 녹음 중단 · 번역 실패 등)을 상세와 내보내기에 남깁니다
- 배너를 공용 컴포넌트로 통일하고 경고 색을 하나로 정리했습니다
- `RetransModel/MODEL_SOURCE.txt` — AI 재번역 모델의 구성·출처를 기록(모델 가중치는 저장소에 포함되지 않습니다)

### ℹ️ 알려진 동작
- 화자 다시 인식의 **자동 인원 추정**은 멀리서 녹음한 대면 대화에서 여러 사람을 한 명으로 묶을 수 있습니다 — 인원을 지정해 실행하는 편이 정확합니다
- 재전사 대본은 "정답"이 아니라 대조용입니다. 라이브와 같은 환각 필터·언어 클램프를 거치지만 소음 구간은 파편으로 남을 수 있습니다

## v0.7.0 — 2026-07-30

### ✨ 새 기능
- **세션 음원 녹음** — 대면 대화·온라인 미팅의 소리를 세션마다 WAV(16비트)로 저장합니다. 원음(장치가 준 그대로)과 인식 음원(엔진이 실제로 소비한 신호)을 함께 남겨, 자막이 틀렸을 때 원인을 나중에 확인할 수 있습니다
  - 라이브 화면에 **녹음 저장 중 / 꺼짐 / 실패** 칩이 세션 내내 표시됩니다
  - 세션을 삭제하면 녹음도 함께 삭제됩니다(삭제 확인 창에 파일 수·용량 표시)
  - 설정 › 녹음에서 켜고 끄며 총 사용량과 Finder 바로가기를 제공합니다
  - 앱이 강제 종료돼 파일을 닫지 못해도 다음 실행 때 헤더를 자동 복구해 오디오가 살아납니다
  - 세션 도중 입력 장치가 바뀌면(에어팟 배터리 방전 등) 새 파일로 이어서 저장합니다

### ℹ️ 알려진 동작
- 녹음은 자동으로 지우지 않습니다. 자막이 한 줄도 없어 저장되지 않은 세션의 녹음도 남습니다(분석용)

## v0.6.0 — 2026-07-25

### 🐛 수정
- **대면 대화에서 앞사람의 답변이 자막에 나오지 않던 문제** — 첫 문장(내 발화)은 인식되는데 상대의 답변은 통째로 사라지던 현상입니다. 전사 게이트가 절대 음량 기준이라 1 m 이상 떨어진 목소리를 소음으로 버리고 있었습니다. 이제 음성 분류(사람 말인가)로 판정해 원거리 발화도 받습니다. 조용한 방에서 없는 자막이 생기지 않도록 환각 방어는 그대로 유지됩니다
- 창이 무한히 커지며 자막이 멈추던 경로를 닫았습니다
- 온라인 미팅의 **내 마이크** 스트림은 근접 발화만 받아 다른 사람 말이 `나`로 기록되지 않게 합니다

## v0.5.0 — 2026-07-24

### ✨ 새 기능
- **온라인 미팅에서 내 발화도 자막·녹취** — 온라인 미팅 모드가 상대 소리(시스템 오디오)와 내 마이크를 동시에 캡처합니다. 내 발화는 `나`로 고정 표기되고, 상태바의 **내 마이크** 토글로 끌 수 있습니다(타이핑 발화는 토글과 무관)
- **동시 발화 표시** — 상대와 내가 동시에 말하면 잠정 줄 2개(상대 크게 · 나 한 단계 작게)
- **레벨 미터 2계통**(마이크 파랑 · 시스템 초록)과 조건부 배너 2종 — 내장 스피커 사용 중 경고, 마이크 권한 없음 안내(상대 소리는 계속 자막)

## v0.4.0 — 2026-07-23

### ✨ 새 기능
- **AI 재번역** — 세션이 끝나면 앱에 내장된 온디바이스 언어 모델이 문맥을 보며 다시 번역합니다. 실시간 번역은 지우지 않고 별도로 보관해 토글로 비교할 수 있고, 줄에 마우스를 올리면 원래 번역이 취소선으로 보입니다. 재번역된 줄은 ✦ 마커, 미재번역 줄은 기존 표기 그대로
  - 진행률(n/N)과 취소, 실패 시 "실시간 번역을 표시하고 있어요" 배너
  - 내보내기에서 번역본을 선택할 수 있고 AI 재번역을 쓰면 문서에 표기가 강제됩니다
  - 회의록 → 재번역 순으로 직렬 실행해 메모리 동시 점유를 피합니다

## v0.3.0 — 2026-07-19

### ✨ 새 기능
- **iPad(대면 대화 전용) 유니버설 타깃** — 같은 코드로 iPad를 지원합니다. 시스템 오디오 모드와 팝아웃은 iPad에서 제외됩니다
- **실시간 번역 고품질 전략** — Apple Translation의 `highFidelity` 전략을 사용합니다(지원되지 않는 환경은 기본 전략으로 폴백)

### ✨ 변경
- 내보내기 파일 생성 경로를 macOS/iOS 공용으로 단일화(PDF 렌더 공용, iOS는 공유 시트)

## v0.2.0 — 2026-07-18

### ✨ 새 기능
- **온보딩** — 첫 실행에서 환영 → 마이크 권한 사전 설명 → 언어팩 준비(한국어·영어·인도네시아어 순차 다운로드)
- **기록 보관함** — 전체 세션 카드와 전문 검색(제목·미리보기·녹취록 원문·번역)
- **세션 제목 인라인 편집** — 상세 헤더의 제목을 클릭해 바로 수정
- **설정 화면** — 밀도·글자 크기·불투명도·이중 표기·창 가림 자동 표시를 영구 저장
- **DOCX 내보내기**
- **팝아웃 자동 표시** — 메인 창이 가려지거나 최소화되면 자막 패널을 자동으로 띄우고, 돌아오면 닫습니다
- **'새 자막 N개' 배지** — 위로 스크롤해 읽는 중 도착한 자막 안내
- **회의록 참여 인원 이름 정정** — 못 맞힌 자리는 추정 역할을 보여주고 클릭해 바로 고칩니다

### 🐛 수정
- 기록을 전부 삭제한 뒤 재실행하면 예제 세션이 되살아나던 문제
- 화자 경계에서 다음 문장의 첫 단어가 잘리던 문제 — 확정 시 창 끝을 겹쳐 남기는 방식으로 어두를 보존

## v0.1.0 — 2026-07-17

### ✨ 첫 공개
- 홈(언어 1·2 드롭다운, 캡처 모드 2×2 타일) · 라이브(전체 창 자막 + 타이핑 발화 + 읽어주기) · 세션 상세(녹취록·회의록) · 자막 패널 팝아웃
- 실시간 자막 — 마이크(Apple SpeechTranscriber) · 시스템 오디오(ScreenCaptureKit) · 인도네시아어(WhisperKit large‑v3 turbo)
- 실시간 번역(Apple Translation) · 화자 분리(FluidAudio LS‑EEND) · 회의록 자동 작성(FoundationModels, 근거 타임코드 칩·확인 필요 배지)
- 내보내기 TXT · Markdown · PDF
- 무음 후 자막이 멈추던 문제 — 오디오 구성 변경 시 엔진을 재구성하는 워치독으로 해결
- 환각 방어 — 신뢰도 필터 · 상용구 사전 · 발화량 게이트 · 음성 분류 게이트 · 어두 프리롤
